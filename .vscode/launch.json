{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run active DSA file",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/practice/runner.py",
      "args": ["${file}"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
