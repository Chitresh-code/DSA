#include <bits/stdc++.h>
using namespace std;

int main() {
    // list<int> li;

    // li.push_back(2);
    // li.emplace_back(4);

    // li.push_front(5);
    // li.emplace_front(10);

    // for(auto it : li) {
    //     cout << it << " ";
    // }

    // rest of the functions are same as vector

    return 0;
}